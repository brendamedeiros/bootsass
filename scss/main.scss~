@import "compass";
@import "bootstrap";

$menuColor: #000;
$menuHover: #F0F;
body{

	margin:0px;
	padding-top: 70px; 
}
.site-header{
	@include make-row();
	color: white;
	a{
		color: inherit;
		&:hover{
			color: $menuHover;
		}
			
	}
	.site-logo{
		@include make-sm-column(5);
	}
	.animals{
		@include make-sm-column(7);
	}
	.site-nav{
		@include make-sm-column(12);
		color: $menuColor;
	}
}
.site-content{
	@include make-row();
	.featured{
		//@include make-sm-column-push(1);
		@include make-sm-column(12);
		padding: 10px 0;
	}
	.site-widgets{
		@include make-row();
		.widget{
			@include make-sm-column(4);
		}
	}
}

.site-footer{
	@include make-row();
	.site-info{
		@include make-sm-column(5);
		@include make-sm-column-offset(5);
	}
}




.about{
	@include make-sm-column(6);	
}

$white: #FFF;
$blue: #306d9f;
$width: 100%;

@mixin sections-parallax($image, $extension, $height){
	background: url($image + '.' + $extension) no-repeat center center fixed; 
	  display: table;
	  height: $height;
	  position: relative;
	  width: $width;
	  -webkit-background-size: cover;
	  -moz-background-size: cover;
	  -o-background-size: cover;
	  background-size: cover;
}

.home{
	@include sections-parallax(logo, png, 100%);
}
.information{
	@include make-sm-column(6);
	@include sections-parallax(logo, png, 800px);
	.panel {
	  opacity: 0.85;
	}
}

footer {
  padding: 20px 0;
  .glyphicon {
  	color: #333333;
  	font-size: 60px;
   }
   &:hover {
   color: $blue;
   }
}
.services {
  @include make-sm-column(3);
  background-color: $blue;
  color: $white;
 	.glyphicon {
		border: 2px solid $white;
		border-radius: 50%;
		display: inline-block;
		font-size: 60px;
		height: 140px;
		line-height: 140px;
		text-align: center;
		vertical-align: middle;
		width: 140px;
	}
}

.pad-section {
  padding: 50px 0;
	img {
	  width: $width;
	}
}

.text-vcenter {
  display: table-cell;
  text-align: center;
  vertical-align: middle;
  h1 {
	  font-size: 4.5em;
	  font-weight: 700;
	  margin: 0;
	  padding: 0;
	}
}

